Ext.define('ksp.model.Profile', {
    extend: 'Ext.data.Model',

    config: {
        fields: [
            'lable',
            'text',
            'disclosure',
            'xtype'
        ]
    }
});
